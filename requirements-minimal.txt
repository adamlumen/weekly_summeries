# Minimal requirements for quick testing
# Use this for faster initial setup: pip install -r requirements-minimal.txt

# Core dependencies
fastapi>=0.110.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-dotenv>=1.0.0

# OpenAI integration
openai>=1.12.0

# Basic database support
sqlalchemy>=2.0.0
aiosqlite>=0.19.0  # For SQLite (easier for testing)

# Essential utilities
aiofiles>=23.2.0
httpx>=0.27.0
pyyaml>=6.0.0

# MCP (Model Context Protocol)
mcp>=1.12.0
